export const apiUrl = `${window.location.origin}/api`; //  'http://127.0.0.1/api';
export const loginApiUrl = `${apiUrl}/users/login`;
export const registerApiUrl = `${apiUrl}/users/register`;
export const scrollingDelay = 3000;
